# isIOS,新增于v1.0.3

判断是否为isIOS手机，因为某些场景IOS跟安卓有不同的业务逻辑

## 入参(无)


## return

(Boolean): 是否为isIOS手机,是=true,否=false

## example

    const utils = require('jm365@utils')
    console.log(utils.isIOS()) // ture / false
